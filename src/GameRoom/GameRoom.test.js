import React from 'react';
import { render } from '@testing-library/react';
import RoomEntry from '.';

test('shows room code and name entry options', () => {
  // TODO
});

// TODO: test the callbacks
